/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        NEXT_PUBLIC_API_URL: "https://raw.githubusercontent.com/shahadat-repliq/healnet/main/data"
    }
};

export default nextConfig;
